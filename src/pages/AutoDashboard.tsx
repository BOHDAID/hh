import UserLayout from "@/components/user/UserLayout";

const AutoDashboard = () => {
  return (
    <UserLayout title="لوحة النشر التلقائي" subtitle="قريباً...">
      <div className="space-y-6">
        <p className="text-muted-foreground">قريباً...</p>
      </div>
    </UserLayout>
  );
};

export default AutoDashboard;
